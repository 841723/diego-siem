/**
 * @deprecated Use useGlobalMapping instead.
 * The mapping is now a single global resource; this hook is kept only for build
 * compatibility while old pages are redirected away.
 */
export { useGlobalMapping as useMappings } from "./useGlobalMapping";
